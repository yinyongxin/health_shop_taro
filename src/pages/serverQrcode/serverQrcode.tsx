import { BasePage, HospitalList } from "@/components";
import "./wareList.css";

const HospitalListPage = () => {
  return (
    <BasePage fullScreen>
      <HospitalList />
    </BasePage>
  );
};

export default HospitalListPage;
